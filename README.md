Terminal rock paper scissors game. Goal was to write it in one line.

Since we don't want to import `random`, we use the following.

```python
object() # creates a new empty object
id(object) # returns the unique identity number of the object in the memory
```